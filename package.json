{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix frontend start",
    "build": "npm --prefix frontend run build && node scripts/publish-frontend.mjs",
    "test": "npm --prefix frontend test",
    "eject": "npm --prefix frontend eject"
  }
}
